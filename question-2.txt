[1]. take the test case variable.
[2]. run a loop from t to 1.
[3]. take input in 3 array.
[4]. one by one making those array to middle array.
[5]. checking condition for order exist 
     if order exist return 1;
     else 
     swap row(0,2);
     again check condition 
     swap row(0,2); // convert matrix to before condition matrix.
     if b==1 return yes
     else return no
